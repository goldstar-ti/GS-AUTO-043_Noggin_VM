[] csv_importer adding _-_ underscores
[] csv_importer option to import only import tip

[] object_type being written as full object name instead of abbreviation when updating the database records during processing

[] noggin continuous processor
[] system service
[] update docs
[] web interface
[] organise files / tidy up dir (e.g. don't need LCD_ONLY anymore)

[] optimise places where object_type is hardcoded - so that in future only a new config file needs to be added (e.g. want to add Goldstar Illness Injury - GII)

[] what do you want to do menu - 1 import tips, 2 check continous processor status, 3 open web interface